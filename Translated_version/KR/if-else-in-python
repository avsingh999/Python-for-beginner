#특정 조건이 만족되는 경우에만 코드를 실행하려면 의사 결정이 필요합니다.

#간단한 if 문 :-

if test expression:
    statement(s)

#if-Else 문 :-

if test expression:
    Body of if
else:
    Body of else

#Elif (else if 사다리):-

if test expression:
    Body of if
elif test expression:
    Body of elif
else: 
    Body of else
